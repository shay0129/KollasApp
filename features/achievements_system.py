class AchievementsManager:
    def __init__()
    - create_achievement(name, description, criteria) - יצירת הישג חדש
    - check_achievement_criteria(user_id, achievement_id) - בדיקה האם המשתמש השיג את היעד
    - award_achievement(user_id, achievement_id) - הענקת הישג למשתמש
    - get_user_achievements(user_id) - קבלת כל ההישגים של משתמש
    - get_available_achievements() - קבלת רשימת כל ההישגים האפשריים
    - track_user_progress(user_id, activity_type, value) - מעקב אחר התקדמות המשתמש